import styled from "styled-components";

const Pied = () => {
    return (
        <Wrapper>CalcGluten, une idée originale de Nicola</Wrapper>
    )
}

const Wrapper = styled.footer`
    background-color: var(--c4);
    height: 100px;
`

export default Pied;